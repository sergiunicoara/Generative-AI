# Host port 7688, not the Neo4j default 7687 — see docker-compose.yml's neo4j
# service comment (this machine already runs another project's Neo4j on 7687).
NEO4J_URI=bolt://localhost:7688
NEO4J_USER=neo4j
NEO4J_PASSWORD=scg_dev_local

# LLM provider for structured extraction (P3) — not yet decided which.
LLM_PROVIDER=
LLM_API_KEY=

# Embedding provider — Part 7 item 6 in docs/plan.md is still open.
EMBEDDING_PROVIDER=
EMBEDDING_API_KEY=
