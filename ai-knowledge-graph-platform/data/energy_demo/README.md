# Synthetic source exports

All files in this directory are fictional fixtures. `snowflake_observations.json`
models a Snowflake-style telemetry extract. The authoritative in-code fixture
also models SAP-style assets/work orders and SharePoint-style document revisions
so the demo stays deterministic without external credentials.
