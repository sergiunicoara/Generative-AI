PREFIX energy: <https://example.energy.demo/ontology#>
SELECT ?asset ?temperature ?threshold WHERE {
  ?observation a energy:Observation ; energy:observedAsset ?asset ; energy:metric "temperature_c" ; energy:value ?temperature .
  ?bulletin a energy:DocumentRevision ; energy:temperatureReviewThreshold ?threshold .
  FILTER(?temperature > ?threshold)
}
