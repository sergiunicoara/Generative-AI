# Development and CI dependencies.
#
# These were previously undeclared anywhere: CI ran `pip install -r
# requirements.txt` and then `pytest`, which does not exist in that file — so
# the test job could never have passed. `make install-dev` had the same gap.
#
# Install with:  pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# pytest-asyncio is not optional: pyproject.toml sets asyncio_mode = "auto",
# so every async test in the suite fails to run without it.
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# Property-based testing. Example-based tests pin the cases someone thought
# of; these pin invariants across generated inputs, which is what catches the
# tenant-isolation and escaping edge cases nobody enumerates by hand.
hypothesis>=6.100.0

# Mutation testing is intentionally opt-in (too expensive for every PR); the
# focused Make target below guards the newly security-sensitive adapters.
mutmut>=3.2.0

ruff>=0.6.0
pip-tools>=7.4.0
