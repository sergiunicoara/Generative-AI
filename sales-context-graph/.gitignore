.env
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
*.egg-info/
dist/
build/
.coverage
htmlcov/

# Phase 10 (loadtest/) -- dated per-run baseline reports/logs, not source.
loadtest/results/

# scripts/backup_neo4j.sh's output -- local dev-DB tarballs (tens of MB each,
# regenerated on demand), not source. See scripts/backup_neo4j.sh's header.
backups/

# Presentation renderer intermediates (the final MP4 is intentionally tracked).
artifacts/presentation_video_work/

# HTTrack mirror of the deployed demo site (scraper output, not source --
# includes hts-cache/ and a captured Cloudflare __cf_bm cookie). Regenerate
# with HTTrack if a local copy is ever needed again; never track it.
site/
